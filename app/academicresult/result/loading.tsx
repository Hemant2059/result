import { Button } from "@/components/ui/button";
import React from "react";

const loading = () => {
  return (
    <div className="h-screen flex items-center justify-center">
      <Button>Wait ASSHOLE</Button>
    </div>
  );
};

export default loading;
